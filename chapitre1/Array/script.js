var transactions = [];

// Init app
function init() {
  transactions.push("virement d'un client ");
  transactions.push("payer facture d'eau'");
  transactions.push("acheter un pc");
  transactions.push("payer facture de gaz ");
  console.log(transactions);

  // ici des exemple de gestion du tableau
}

// call init
init();
